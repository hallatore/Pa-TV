<UserControl
    x:Class="Pa_TV.UserControls.Event"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Pa_TV.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="80"
    d:DesignWidth="400"
    HorizontalAlignment="Left">
    
    <Border x:Name="BorderControl" BorderThickness="1" BorderBrush="#FF333333">
        <Grid Margin="12,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Farmen" Style="{StaticResource GroupHeaderTextStyle}"/>
            <TextBlock Grid.Row="1" Margin="0,6,0,0" Text="07:30" Style="{StaticResource SubtitleTextStyle}"/>
        </Grid>
    </Border>
</UserControl>
